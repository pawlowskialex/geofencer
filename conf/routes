# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST        /log           controllers.Application.submitLogEntry
GET         /events        controllers.Application.listEvents(after: Long)